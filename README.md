# cricscores-with-python
Get cricket scores in your terminal

Run with the command python3 cricscores.py

Linux users can create a symbolic link with the command
sudo ln -s cricscores.py /usr/local/bin/
